import { Meta, Story } from '@storybook/react/types-6-0' __teste__
import { {{ properCase name }}, {{ camelCase name }}Props } from './{{ snakeCase name }}'
__importMock__

export default {
  title: '{{ titleStorybook }}',
  component: {{ properCase name }},
  args: __hasMock__
} as Meta
 // {{ components_choices }}
 export const Template: Story<{{ camelCase name }}Props > = (args) => <{{ properCase name }} {...args} />

Template.parameters = {
  backgrounds: {
    default: 'ligth'
  },
}
