import { renderTheme } from '../../styles/render-theme'
import { NavMenuSidebar } from './nav_menu_sidebar'
describe('<NavMenuSidebar />', () => {
  it('should render', () => {
    renderTheme(<NavMenuSidebar />)
  })
})
