import { renderTheme } from '../../styles/render-theme';
import { ContainerPost } from './container_post';
describe('<ContainerPost />', () => {
  it('should render', () => {
    renderTheme(<ContainerPost />)
  })
})
