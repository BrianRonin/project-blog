import styled, { css, DefaultTheme } from 'styled-components'
import { {{ camelCase name }}Props } from './{{ snakeCase name }}'
??typeStyle??

export const Main = styled.{{ htmlElement }}<??propTypeStyle??>`
  ${({ theme }) => css`
    //
  `}
`
