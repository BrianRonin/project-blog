import styled, {
  css,
  DefaultTheme,
} from 'styled-components'
import { postProps } from './Post'
// type S_postProps = Exclude<
//   postProps
// >

export const Main = styled.div`
  ${({ theme }) => css`
    //
  `}
`
